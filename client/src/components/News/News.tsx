import {ComponentWithMeta} from "../../misc/ComponentWithMeta.ts";

export const News: ComponentWithMeta  = () => {
    return (
        <div>

        </div>
    );
};

News.meta = {
    route: "news",
    roles: [""],
}